# Your Anthropic API key — the proxy holds this so it never reaches the browser.
ANTHROPIC_API_KEY=

# Port the proxy listens on (default 8787). Point the frontend at this:
#   in the app root, set VITE_API_BASE=http://localhost:8787
PORT=8787
